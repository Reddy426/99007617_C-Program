#include "Ascii.h"
int Add(int i)
{
        Name.len = strlen(Name.string1);
  for (i = 0; i < Name.len;i++)
   {
      Name.sum = Name.sum + Name.string1[i];
   }
    printf("\nSum of all characters : %d\n ",Name.sum);
}